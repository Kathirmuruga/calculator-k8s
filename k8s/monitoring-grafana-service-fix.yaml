apiVersion: v1
kind: Service
metadata:
  name: monitoring-grafana
  namespace: monitoring
spec:
  selector:
    app.kubernetes.io/name: grafana
    app.kubernetes.io/instance: monitoring
  ports:
    - port: 80
      targetPort: 3000   # changed from 'grafana' to numeric container port
  type: ClusterIP
